import XCTest
@testable import SVGWrapper

final class SVGWrapperTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(SVGWrapper().text, "Hello, World!")
        _ = {}()
    }

    // static var allTests = [
    //     ("testExample", testExample),
    // ]
}
